package main

//Assign int value to variable
var defaultOffset = 10

func main() {

	//Assign int value to variable
	offset := defaultOffset
	//Print the first value
	println(offset)
	//Changing the value of the value
	offset = 10 + defaultOffset
	//Printing the latest change
	println(offset)
}
